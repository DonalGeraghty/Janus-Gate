name: Deploy to Google Cloud Run

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch: # Allow manual trigger

env:
  PROJECT_ID: donal-geraghty-home
  SERVICE_NAME: janus-api
  RUNTIME_SERVICE_ACCOUNT: janus-api
  # Retained so credentials encrypted before the rename remain decryptable.
  KMS_KEYRING: janus-gate
  REGION: europe-west1

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    env:
      PUSH_CRON_SECRET: ${{ secrets.PUSH_CRON_SECRET }}
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: Validate imports
        run: |
          python -c "from services.firebase_service import initialize_firebase; print('✅ Firebase service imports successful')"
          python -c "from services.logging_service import logger; print('✅ Logging service imports successful')"
          python -c "from core.auth_service import register_user, login_user; print('✅ Auth service imports successful')"
          python -c "from app import app; print('✅ Flask app imports successful')"

      - name: Run tests
        env:
          FLASK_ENV: development
          JWT_SECRET_KEY: test-secret
        run: python -m unittest discover -s tests -v
      
      - name: Google Auth
        id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
          project_id: ${{ env.PROJECT_ID }}
          
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        
      - name: Configure Docker
        run: gcloud auth configure-docker
        
      - name: Set project
        run: gcloud config set project ${{ env.PROJECT_ID }}
        
      - name: Enable APIs
        run: |
          gcloud services enable run.googleapis.com
          gcloud services enable firestore.googleapis.com
          gcloud services enable cloudbuild.googleapis.com
          gcloud services enable cloudkms.googleapis.com
          gcloud services enable cloudscheduler.googleapis.com
          
      - name: Create service account if not exists
        run: |
          if ! gcloud iam service-accounts describe ${{ env.RUNTIME_SERVICE_ACCOUNT }}@${{ env.PROJECT_ID }}.iam.gserviceaccount.com &> /dev/null; then
            gcloud iam service-accounts create ${{ env.RUNTIME_SERVICE_ACCOUNT }} \
              --display-name="Janus API"
          fi
          
      - name: Grant Firestore permissions
        run: |
          gcloud projects add-iam-policy-binding ${{ env.PROJECT_ID }} \
            --member="serviceAccount:${{ env.RUNTIME_SERVICE_ACCOUNT }}@${{ env.PROJECT_ID }}.iam.gserviceaccount.com" \
            --role="roles/datastore.user"
          gcloud projects add-iam-policy-binding ${{ env.PROJECT_ID }} \
            --member="serviceAccount:${{ env.RUNTIME_SERVICE_ACCOUNT }}@${{ env.PROJECT_ID }}.iam.gserviceaccount.com" \
            --role="roles/datastore.viewer"

      - name: Grant KMS key permissions
        run: |
          gcloud kms keys add-iam-policy-binding user-openai-keys \
            --keyring="${KMS_KEYRING}" \
            --location="${REGION}" \
            --member="serviceAccount:${RUNTIME_SERVICE_ACCOUNT}@${PROJECT_ID}.iam.gserviceaccount.com" \
            --role="roles/cloudkms.cryptoKeyEncrypterDecrypter"
            
      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --source . \
            --region ${{ env.REGION }} \
            --service-account ${{ env.RUNTIME_SERVICE_ACCOUNT }}@${{ env.PROJECT_ID }}.iam.gserviceaccount.com \
            --allow-unauthenticated \
            --port 8080 \
            --memory 512Mi \
            --cpu 1 \
            --max-instances 10 \
            --timeout 300 \
            --set-env-vars "FLASK_ENV=production,JWT_SECRET_KEY=${{ secrets.JWT_SECRET_KEY }},OPENAI_MODEL=gpt-5.6-sol,MISTRAL_MODEL=mistral-small-2603,ANTHROPIC_MODEL=claude-sonnet-5,AI_KMS_KEY_NAME=projects/${{ env.PROJECT_ID }}/locations/${{ env.REGION }}/keyRings/${{ env.KMS_KEYRING }}/cryptoKeys/user-openai-keys,VAPID_PRIVATE_KEY=${{ secrets.VAPID_PRIVATE_KEY }},VAPID_PUBLIC_KEY=${{ secrets.VAPID_PUBLIC_KEY }},VAPID_SUBJECT=${{ secrets.VAPID_SUBJECT }},PUSH_CRON_SECRET=${{ secrets.PUSH_CRON_SECRET }}"
            
      - name: Get service URL
        id: service-url
        run: |
          URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} --region ${{ env.REGION }} --format="value(status.url)")
          echo "url=$URL" >> $GITHUB_OUTPUT
          
      - name: Test deployment
        run: |
          echo "Testing deployed service at ${{ steps.service-url.outputs.url }}"
          curl -f ${{ steps.service-url.outputs.url }}/health || exit 1

      - name: Configure push reminder scheduler
        if: ${{ env.PUSH_CRON_SECRET != '' }}
        run: |
          JOB_NAME="nyx-push-reminders"
          JOB_URI="${{ steps.service-url.outputs.url }}/api/internal/push/reminders"
          if gcloud scheduler jobs describe "$JOB_NAME" --location="${{ env.REGION }}" >/dev/null 2>&1; then
            gcloud scheduler jobs update http "$JOB_NAME" \
              --location="${{ env.REGION }}" \
              --schedule="*/5 * * * *" \
              --uri="$JOB_URI" \
              --http-method=POST \
              --headers="X-Cron-Secret=$PUSH_CRON_SECRET"
          else
            gcloud scheduler jobs create http "$JOB_NAME" \
              --location="${{ env.REGION }}" \
              --schedule="*/5 * * * *" \
              --uri="$JOB_URI" \
              --http-method=POST \
              --headers="X-Cron-Secret=$PUSH_CRON_SECRET"
          fi
          
      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `🚀 **Deployment successful!**\n\nAuth API live at:\n${{ steps.service-url.outputs.url }}\n\n**Endpoints:**\n- POST ${{ steps.service-url.outputs.url }}/api/auth/register\n- POST ${{ steps.service-url.outputs.url }}/api/auth/login\n- GET ${{ steps.service-url.outputs.url }}/api/auth/me\n- GET ${{ steps.service-url.outputs.url }}/health`
            })
